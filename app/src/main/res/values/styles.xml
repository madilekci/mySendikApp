<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/secondaryColor</item>
    </style>

    <!-- Custom Toolbar style. -->
    <style name="CustomToolbarStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/primaryColor</item>
        <item name="android:textColor">@color/primaryLightTextColor</item>
    </style>

    <!-- Custom Option style. -->
    <style name="CustomOptionStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/color10</item>
    </style>

    <!-- SplashScreen theme. -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/backgroundsplash</item>
    </style>

    <!-- Switch Buttons Style-->
    <style parent="Theme.AppCompat.Light" name="switchButtonStyle">

        <item name="colorControlActivated">@color/secondaryLightColor</item>
        <item name="colorSwitchThumbNormal">@color/defBlueGray</item>
        <item name="android:colorForeground">@color/secondaryDarkColor</item>

    </style>

</resources>
