<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ll_rv_one1"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:background="#9999">

    <android.support.v7.widget.CardView
        android:id="@+id/cv_rv_one"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dim_rv_one_Fullheight"
        android:layout_margin="3dp"
        android:background="#9999"
        app:cardCornerRadius="10dp">


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">


            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="187dp"
                android:orientation="vertical">

                <ImageView
                    android:id="@+id/NewsFeedPic"
                    android:layout_width="@dimen/dim_rv_one_Imagesize"
                    android:layout_height="@dimen/dim_rv_one_Imagesize"
                    android:layout_margin="2dp"
                    android:background="@color/colorPrimaryDark"
                    android:scaleType="fitXY"

                    />

                <Button
                    android:id="@+id/rv_one_btn_view"
                    android:layout_width="149dp"
                    android:layout_height="39dp"
                    android:layout_margin="2dp"
                    android:background="@color/common_google_signin_btn_text_light_pressed"
                    android:text="362 kez görüntülendi"
                    android:textColor="@android:color/white"
                    android:textSize="10sp"

                    />

            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tv_rv_one_title"
                    android:layout_width="match_parent"
                    android:layout_height="50dp"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:padding="5sp"

                    android:text="1312312"
                    android:textAllCaps="true"

                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/tv_rv_one_summary"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="6"
                    android:padding="5sp"
                    android:text="Supposedly android:maxLines= and android:singleLine= should do basically android:maxLines= and android:singleLine= should do basicalthe same thing and since singleLine is apparently deprecated I would prefer not to use it, but when I take it out, the marquee doesn't scroll anymore. Taking maxLines out doesn't affect it, Supposedly android:maxLiand android:singleLine=should do basically the same thing and since singleLine is apparently deprecated I would prefer not to use it, but when I take it out, the marquee doesn't scroll anymore. Taking maxLines out doesn't affect it, though.  Related:though.  Related:"
                    />

            </LinearLayout>
        </LinearLayout>
    </android.support.v7.widget.CardView>




</LinearLayout>